rootProject.name = 'modulism'

include(
  'operations:operations-api',
  'operations:operations-api-impl',
  'tax:tax-api',
  'tax:tax-api-impl',
  'report',
  'shared'
)

includeBuild 'event-publisher'
